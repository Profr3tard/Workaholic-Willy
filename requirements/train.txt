# Offline model-training stack (NOT a runtime-serving dependency).
#
# scikit-learn powers the OFFLINE trainers only — the U1 success-probability
# model, the V-phase candidate/ranking/recovery/OPE policies, and their
# `python -m backend.src.robot.grasping.rl <train-*|ope|promote-policy>` CLIs.
# The RUNTIME success predictor is deliberately sklearn-free (late, scoped import
# in scoring/success_probability.py), so this is intentionally kept OUT of base.txt.
#
# Required by CI (the U1/U3/V* training + determinism tests import it) — pulled in
# via requirements/dev.txt — and on any workstation that regenerates model artifacts.
scikit-learn==1.9.0
