# OPTIONAL — Intel RealSense RGB-D camera stack. NOT installed by default and
# deliberately excluded from CI (no depth camera on the runners), so macOS /
# Linux runners without librealsense stay green. Install on a machine with a
# RealSense device attached:
#   pip install -r requirements/camera-realsense.txt
#
# pyrealsense2 (librealsense Python bindings) is used by:
#   backend/src/camera/setup/image_taking/rgbd.py  (RealSenseRGBDStreamer)
# The import is deferred, so the library runs fine without this layer; only a
# camera rig with `rgbd_backend: realsense` needs it. Pin the exact patch
# release to the librealsense build that matches your device firmware.
pyrealsense2>=2.54
