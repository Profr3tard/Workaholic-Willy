# OPTIONAL — voice/gesture surfaces, NOT part of the grasp pipeline.
#
# Workaholic-Willy's grasping path never imports these. They power the standalone
# hand/gesture detectors (backend/src/models/handdetection/, MediaPipe) and the
# speech-to-text helper (backend/src/models/speech/, which also uses the
# already-bundled torch/transformers/sounddevice). Kept out of the default
# install so a host that only runs grasping is not forced to pull MediaPipe.
# The modules import cleanly without this extra and raise a clear, actionable
# error only when a detector is actually constructed.
mediapipe==0.10.21
