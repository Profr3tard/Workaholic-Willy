# ──────────────────────────────────────────────────────────────────────────
# Workaholic-Willy is a library + CLI stack.
#
# From the creators of Willy the Workaholic, the library contains a set 
# of tools to create beautiful and functional robot cells. All powered by
# the latest AI technologies.
# ──────────────────────────────────────────────────────────────────────────
pydantic==2.13.4
PyYAML==6.0.3
numpy==2.4.4
scipy==1.17.1
pillow==12.2.0
opencv-contrib-python==4.13.0.92
open3d==0.18

# WARNING: If on Linux:install it: `apt-get install -y libportaudio2`.
# Without it, the top-level `import sounddevice`
# in models/speech/speech_to_text.py raises OSError.
sounddevice==0.5.5 
matplotlib==3.10.8
transformers==5.5.4

# Torch and torchvision are pinned with CUDA support for GPU acceleration.
# This is the CPU-only version of the requirements file.
# If you want to use GPU acceleration, please refer to requirements.txt.
torch==2.7.1
torchvision==0.22.1

# Camera support for Intel RealSense cameras.
# We highly recommend using a Intel RealSense camera for 3D perception tasks.
pyrealsense2>=2.54

# Robot specific dependencies
# For UR robots, we use the `ur_rtde` library to communicate with the robot
# For KUKA robots, we use the `kuka-iiwa` library to communicate with the robot.
ur_rtde==1.6.5
#kuka-iiwa==0.1.0
python-fcl==0.7.0.11 # Fallback to `python-fcl` if `COAL` is not available.

# Signing and authentication of training data and models.
# We use SHA256 for signing and verifying the integrity of data and models.
cryptography==45.0.7
boto3>=1.34 # For AWS S3 support. AZURE and GCP support is coming soon.

# Training and inference dependencies
scikit-learn==1.9.0
accelerate==1.11.0

# Handrecognition dependencies
mediapipe==1.0.1

# API dependencies for FASTAPI and REST API support.
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# Development dependencies
ruff==0.15.15
mypy==2.1.0
pytest==9.0.3
pytest-cov==7.1.0
pip-audit